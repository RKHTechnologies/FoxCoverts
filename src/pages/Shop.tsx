import React, { FC } from 'react';
import { PageBodyContainer } from '../Shared/SharedStyles';

const Shop: FC = () => {
  return (
    <PageBodyContainer>
      Shop
    </PageBodyContainer>
  );
}

export default Shop;